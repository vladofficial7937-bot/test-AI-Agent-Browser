# 🚀 Quick Start Guide

## Установка за 5 минут

### 1️⃣ Собрать Extension
```bash
npm install
npm run build
```

### 2️⃣ Настроить и запустить Proxy
```bash
cd agent-proxy
npm install
cp env.example .env
# Отредактируйте .env и добавьте LLM_API_KEY (см. ниже)
npm run build
npm start
```

**Как получить API ключ (выберите один вариант):**

**A) OpenRouter (Рекомендуется)** 🌟
1. Зайдите на https://openrouter.ai
2. Зарегистрируйтесь
3. Создайте API ключ
4. В `.env` укажите:
```env
LLM_API_KEY=sk-or-v1-ваш_ключ
LLM_MODEL=anthropic/claude-3.5-sonnet
LLM_BASE_URL=https://openrouter.ai/api/v1
```

**B) OpenAI напрямую**
1. Зайдите на https://platform.openai.com
2. Создайте API ключ
3. В `.env` укажите:
```env
LLM_API_KEY=sk-ваш_ключ
LLM_MODEL=gpt-4o
LLM_BASE_URL=https://api.openai.com/v1
```

**C) Z.AI (Китайский сервис)**
1. Зайдите на https://open.bigmodel.cn
2. В `.env` укажите:
```env
LLM_API_KEY=ваш_ключ
LLM_MODEL=glm-4-flash
LLM_BASE_URL=https://open.bigmodel.cn/api/paas/v4
```

Должно появиться:
```
Server running on: http://localhost:3131
```

### 3️⃣ Загрузить в Chrome
1. Откройте `chrome://extensions`
2. Включите **Developer mode** ⚙️
3. Нажмите **Load unpacked**
4. Выберите папку `dist/`

### 4️⃣ Проверить соединение
1. Кликните на иконку расширения
2. В Side Panel поле "Agent Proxy" должно показывать `http://localhost:3131`
3. Нажмите **Test** - должно показать "✓ Connection successful!"

## ✅ Готово!

Теперь можете открыть любую страницу и попросить агента выполнить задачу.

## 🧪 Тестовые задачи

### Простая задача (хорошо для первого теста):
```
Собери все контактные данные с этой страницы
```

### Задача с навигацией:
```
Найди раздел pricing и выпиши все тарифы
```

### Задача с формой:
```
Найди форму подписки и заполни email: test@example.com (не отправляй)
```

## 📁 Структура проекта

```
browser 2.0/
├── src/                               # Chrome Extension
│   ├── background/serviceWorker.ts    # Agent loop + security
│   ├── content/contentScript.ts       # DOM automation
│   ├── panel/panel.*                  # UI
│   ├── llm/proxyClient.ts             # Proxy client
│   └── shared/types.ts                # TypeScript types
├── agent-proxy/                       # Proxy Server
│   ├── src/server.ts                  # Express API
│   ├── src/llmClient.ts               # Universal LLM client
│   └── .env                           # API keys (создайте из env.example)
├── manifest.json                      # Chrome extension config
└── dist/                              # Built extension (after npm run build)
```

## 🔧 Development

**Extension (watch mode):**
```bash
npm run dev
```

**Proxy Server:**
```bash
cd agent-proxy
npm run dev
```

Держите оба процесса запущенными во время разработки.

## 🐛 Troubleshooting

**Расширение не работает?**
- Проверьте что proxy server запущен: открыть `http://localhost:3131/health` в браузере
- Проверьте что `npm run build` завершился успешно
- Проверьте что Proxy URL настроен в extension
- Откройте Service Worker console в `chrome://extensions`
- Проверьте логи proxy server в терминале

**Агент не находит элементы?**
- Убедитесь что страница полностью загружена
- Попробуйте сначала простую задачу на знакомом сайте

## 📖 Полная документация

Смотрите [README.md](README.md) для детальной информации.
