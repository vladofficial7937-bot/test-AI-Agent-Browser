# AI Web Agent - Полная настройка

## 🚀 Быстрый старт

### 1. Установка зависимостей

```powershell
# Установить зависимости для расширения
cd "d:\browser 2.0"
npm install

# Установить зависимости для прокси
cd agent-proxy
npm install
```

### 2. Настройка GigaChat API

1. Скопируйте ваш Authorization key от GigaChat
2. Создайте файл `.env` в папке `agent-proxy`:

```env
ZAI_API_KEY=ваш_ключ_здесь
ZAI_MODEL=GigaChat
ZAI_BASE_URL=https://gigachat.devices.sberbank.ru/api/v1

PORT=3131
NODE_ENV=development
```

### 3. Сборка и запуск

#### Автоматический способ (рекомендуется):

```powershell
cd "d:\browser 2.0"
.\rebuild-and-restart.ps1
```

#### Ручной способ:

```powershell
# Собрать расширение
cd "d:\browser 2.0"
npm run build

# Собрать и запустить прокси
cd agent-proxy
npm run build
npm start
```

### 4. Установка расширения в Chrome

1. Откройте `chrome://extensions`
2. Включите "Режим разработчика" (Developer mode)
3. Нажмите "Загрузить распакованное расширение" (Load unpacked)
4. Выберите папку `d:\browser 2.0\dist`

### 5. Использование

1. Откройте любой сайт (например https://hh.ru)
2. Откройте расширение (Side Panel справа)
3. Введите задачу на русском языке
4. Нажмите "Start"

---

## 🔧 Команды для разработки

### Расширение:

```powershell
cd "d:\browser 2.0"

# Сборка
npm run build

# Режим разработки (автопересборка)
npm run watch
```

### Прокси-сервер:

```powershell
cd "d:\browser 2.0\agent-proxy"

# Сборка
npm run build

# Запуск
npm start

# Разработка (автопересборка + перезапуск)
npm run dev
```

---

## 📋 Что сделано

### ✅ Исправления ошибок:

1. **Автоматическая инъекция content script** - агент сам загружает скрипт если его нет
2. **Улучшенное определение зацикливания** - умная система определения повторяющихся действий
3. **Поддержка навигации** - добавлен инструмент `navigate` для перехода между сайтами
4. **Многошаговые задачи** - увеличено до 100 шагов, улучшена обработка истории

### ✅ Многошаговость:

- **100 шагов** - достаточно для сложных задач
- **Умное определение зацикливания:**
  - Обнаруживает 3 одинаковых действия подряд
  - Обнаруживает 5 повторений в последних 7 действиях
  - Отслеживает последние 10 действий
- **Автономный режим** - не требует подтверждения действий
- **Быстрое выполнение** - задержка 300ms между действиями

### ✅ Интеграция с GigaChat:

- OAuth2 автоматическая авторизация
- Кэширование токенов (30 минут)
- SSL сертификат настроен
- Бесплатный API доступен из России

---

## 🎯 Примеры задач

### Простые:

- "Найди 3 вакансии программиста"
- "Открой википедию и найди статью про AI"
- "Найди цену на iPhone 15"

### Многошаговые:

- "Открой hh.ru, найди вакансии Python разработчика в Москве, покажи первые 5"
- "Открой avito.ru, найди iPhone 14, отфильтруй по цене до 50000, покажи топ-3"
- "Открой википедию, найди статью про Россию, перейди в раздел История, прочитай первый абзац"

---

## 🐛 Устранение неполадок

### Расширение не работает:

1. Обновите расширение в `chrome://extensions` (кнопка ↻)
2. Перезапустите прокси-сервер
3. Закройте старые вкладки и откройте новую
4. Проверьте что сервер работает: http://localhost:3131/health

### Content script ошибки:

- Агент теперь **автоматически** инжектит content script
- Если ошибка сохраняется - обновите страницу (F5)
- Не работает на chrome:// страницах - откройте обычный сайт

### Прокси-сервер не запускается:

```powershell
# Найти и убить процесс на порту 3131
Get-NetTCPConnection -LocalPort 3131 | ForEach-Object { 
    Stop-Process -Id $_.OwningProcess -Force 
}

# Запустить снова
cd "d:\browser 2.0\agent-proxy"
npm start
```

### GigaChat ошибки 401:

- Проверьте что ключ правильный в `.env`
- Токены кэшируются на 30 минут
- Перезапустите сервер для обновления токена

---

## 📚 Архитектура

```
d:\browser 2.0/
├── src/                    # Исходный код расширения
│   ├── background/         # Service Worker (агент)
│   ├── content/           # Content Script (DOM взаимодействие)
│   ├── panel/             # Side Panel UI
│   ├── llm/               # LLM клиент
│   └── shared/            # Общие типы
├── agent-proxy/           # Node.js прокси-сервер
│   ├── src/
│   │   ├── server.ts      # Express API
│   │   ├── llmClient.ts   # GigaChat клиент
│   │   └── gigachat.ts    # OAuth2 для GigaChat
│   └── .env               # Конфигурация (API ключ)
└── dist/                  # Собранное расширение
```

---

## 🔐 Безопасность

- **Security Gate** - предотвращает деструктивные действия
- **Prompt injection защита** - игнорирует инструкции со страниц
- **Подтверждение действий** - опционально (по умолчанию выключено)

---

## 📞 Поддержка

Если что-то не работает:

1. Запустите `rebuild-and-restart.ps1`
2. Обновите расширение в Chrome
3. Проверьте логи в консоли браузера (F12)
4. Проверьте логи прокси-сервера

---

**Готово! Агент настроен и готов к работе.** 🎉
