# OpenAI API Key (for local development/testing only)
# In production, API key is stored in chrome.storage.local via UI
# OPENAI_API_KEY=sk-...your-key-here

# Note: This .env file is for reference only
# The extension does NOT read from .env files
# API key must be set through the extension's UI
